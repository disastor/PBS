# pbs (component)

Mock component repo for the HPE Unify RO Application/Release demo.
Real, standalone Unify Component: own repo, own build workflow, own
independently-versioned artifact history — matches TriggerDB's
Products Management model where each product team builds on its own
schedule.

See `.cloudbees/workflows/` for the build/registration workflow.
